spring:
  config:
    import: "optional:configserver:"
  cloud:
    config:
      enabled: false
    aws:
      parameterstore:
        enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

eureka:
  client:
    enabled: false
