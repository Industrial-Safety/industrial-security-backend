package com.industrial.safety.user_service.model;

/** Estado del ciclo de vida de una Solicitud de Acceso (cambio de rol). */
public enum RoleChangeStatus {
    PENDIENTE,
    APROBADA,
    RECHAZADA
}
