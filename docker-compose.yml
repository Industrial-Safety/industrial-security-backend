services:
  mongodb:
    image: mongo:7.0.4
    container_name: mongodb
    ports:
      - "27019:27017" # Asegúrate de que el puerto izquierdo sea el que usas en IntelliJ
    environment:
      - MONGO_INITDB_ROOT_USERNAME=root
      - MONGO_INITDB_ROOT_PASSWORD=password
    volumes:
      - mongo_data:/data/db

volumes:
  course_service_mongo_data: