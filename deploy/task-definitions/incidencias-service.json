{
  "family": "incidencias-service",
  "executionRoleArn": "arn:aws:iam::560904638369:role/ecsTaskExecutionRole",
  "taskRoleArn": "arn:aws:iam::560904638369:role/ecsTaskExecutionRole",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "512",
  "memory": "1024",
  "containerDefinitions": [
    {
      "name": "incidencias-service",
      "image": "PLACEHOLDER",
      "essential": true,
      "portMappings": [
        {
          "containerPort": 8087,
          "protocol": "tcp",
          "name": "incidencias-service-8087-tcp",
          "appProtocol": "http"
        }
      ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/incidencias-service",
          "awslogs-region": "us-east-1",
          "awslogs-stream-prefix": "ecs"
        }
      }
    }
  ]
}
