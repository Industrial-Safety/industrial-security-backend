spring:
  config:
    import: "optional:configserver:"
  cloud:
    config:
      enabled: false
    aws:
      parameterstore:
        enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

eureka:
  client:
    enabled: false

app:
  safety:
    points:
      base: 100
      casco: 20
      guantes: 5
      chaleco: 10
