spring:
  config:
    import: "optional:configserver:"
  cloud:
    config:
      enabled: false
  data:
    mongodb:
      uri: mongodb://root:password@localhost:27020/chat-db?authSource=admin

eureka:
  client:
    enabled: false
